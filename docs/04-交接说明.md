# 04 · 交接说明

> 供下一位接手人快速了解当前状态。功能说明见 [`01-项目说明.md`](01-项目说明.md)，统计口径见 [`02-整体计划.md`](02-整体计划.md)，近期收尾见 [`03-近期计划.md`](03-近期计划.md)，当前有效设计见 [`05-抽奖外移与PAP银行化设计.md`](05-抽奖外移与PAP银行化设计.md)。

---

## 1. 当前状态

- 当前发布版本：`2.0.0`（`main` / `pap-bank` @ `e771de7`）；分支已整理，见 [`版本历史.md`](版本历史.md)
- 测试服历史部署版本：`1f6d70b`（旧分支 `dev-feat/pap-bank-externalize`，已改名；如需对齐请重装 `2.0.0`）
- 测试服 SeAT 根目录：`/var/www/seat`
- 测试服数据库：MariaDB 10.6.x（`.env` 仍是 `DB_CONNECTION=mysql`）
- 目标宿主：SeAT 5.x / Laravel 10.50.x / PHP 8.4.x / Redis

当前主结论：

> `2.0.0`「抽奖重做为行动」主链路已完成测试服验证，并于 2026-06-06 部署到生产；生产执行记录见 §8。

---

## 2. 已完成事项

### 2.1 抽奖外移

- 插件内旧抽奖 UI / controller / route 已退役。
- 侧边栏「超网抽奖」保留为外部跳转入口。
- 跳转 JWT 包含：`sub`、`main_character_id`、`name`、`balance`、`iat`、`exp`。
- 外部抽奖服务负责节点、购买、开奖、中奖记录。

### 2.2 settle 成行动

- 新增：`POST /api/calendar/paps/lottery/settle`。
- settle 会创建一个抽奖 operation：
  - `is_consumption = 1`
  - `consumption_key = lottery-settle:<hash>`
  - `user_id / fc_character_id = 开奖人所属 SeAT user / main character`
  - 自动绑定 `analytics=lottery`、`quantifier=0` 的保留 tag
- 每个参与者写负值 `PapAdjustment(source=lottery, ref_group=...)`。
- `Pap::recomputeValueFor()` 回写 `paps.value`。
- 同场幂等与冲突保护已验证。

### 2.3 行动审查

- 旧外部消费审查页已删除：`/calendar/audit/consumption` 返回 404。
- 行动审查列表显示：普通出勤行动 + 抽奖行动。
- 商店消费锚不显示在行动审查。
- 用户已在 Web UI 确认测试 operation 的 PAP 审查弹窗显示正确。

### 2.4 PAP 统计 / API

- 三口径仍成立：`出勤 PAP - 消费 PAP = 当前可用 PAP`。
- 消费判定以 `calendar_operations.is_consumption` 为准。
- 角色 / 军团 / API 仍按 main character 聚合 alt。
- `GET /api/calendar/paps/{alt}?breakdown=1` 已用测试样本验证：alt 正确归并到 main。

### 2.5 商店预留

- `POST /api/calendar/paps/debit`
- `POST /api/calendar/paps/refund`
- `ApiWriteTokenMiddleware`
- `Operation::standingFor()`

以上保留给未来 PAP 商店，当前抽奖不调用。旧的 `standingFor user_id=0` 外键问题已修复。

---

## 3. 测试服验证记录（2026-06-03）

### 3.1 部署

测试服原先为：

```text
akinams053/seat-pap dev-dev-shop @ 98eab22
```

已切到：

```text
akinams053/seat-pap dev-feat/pap-bank-externalize @ 1f6d70b
```

执行成功：

```bash
sudo composer require akinams053/seat-pap:dev-feat/pap-bank-externalize --no-cache --no-interaction
sudo -u www-data php artisan migrate --force
sudo -u www-data php artisan vendor:publish --force --provider="Seat\Kassie\Calendar\CalendarServiceProvider"
sudo -u www-data php artisan view:clear
sudo -u www-data php artisan route:clear
sudo -u www-data php artisan cache:clear
```

注意：`php artisan` 应以 `www-data` 执行。直接用 `ubuntu` 会因为 `.env` 和 log 权限报错。

### 3.2 迁移

以下迁移已在测试服跑通：

- `2026_05_29_100000_widen_pap_value_precision`
- `2026_05_29_100100_extend_calendar_tags_analytics_enum`
- `2026_05_29_100200_create_lottery_tables`
- `2026_05_30_000000_cleanup_deprecated_notification_settings`
- `2026_06_01_000000_add_bank_ledger_columns`

### 3.3 settle 验证样本

测试 operation：`181`

```text
CLAUDE_VERIFY lottery settle claude-verify-20260603075658
```

持久化结果：

- `is_consumption = 1`
- lottery tag：`PAP 抽奖 / Lottery`
- `paps.value = -0.03`
- 两条 adjustment：
  - `-0.01 CLAUDE_VERIFY alt node`
  - `-0.02 CLAUDE_VERIFY main node`
- alt 与 main 归并到 main character `2121487383`

API 验证：

- 首次 settle：200，`idempotent_replay=false`
- 同 payload 重放：200，`idempotent_replay=true`
- 同 `ref_group` 改 payload：409
- 无 token：401
- 旧 `/calendar/audit/consumption`：404

用户 Web UI 目视验证：

- 类型：抽奖
- 单 PAP：0.00
- 人数：1
- 总额：-0.03
- 成员：weier3
- 奖惩历史正确显示两条负值记录

---

## 4. 已知验证摩擦点

### 4.1 API throttle

插件 API 已改用自有限流器 `calendar-api`（按 IP **300/min**，见 `CalendarServiceProvider::registerApiRateLimiter()`），替代原先继承宿主 `api` 组的 60/min。

判断：

- 正常抽奖 settle 是低频单次调用，远低于上限。
- 外部服务仍应防御性处理 429：读取 `Retry-After`，延迟重试。
- 如果未来余额查询被高频调用，建议用批量接口，并避免轮询。

### 4.2 测试数据

operation `181` 当前保留在测试服，会造成 `weier3` 对应 main character 的 `-0.03` PAP 消费。

是否清理由维护者决定。

---

## 5. 测试服连接约定

统一通过本机集中式 SSH 网关项目 `ssh-gateway`（服务器清单 / 凭据集中在该仓库的 `servers.json` + `keys/`）：

```text
E:\AI\All projects\ssh-gateway        # Windows
/Users/akina/project/ssh-gateway      # mac / Git Bash
```

测试服 target（服务器名 `test` = `ubuntu@ylxh.de:22`）：

```powershell
# Windows
E:\AI\All projects\ssh-gateway\scripts\ssh-gw.cmd test "<command>"
```

```bash
# mac / Git Bash
/Users/akina/project/ssh-gateway/scripts/ssh-gw test '<command>'
```

规约：

- 连接前说明目标是测试服 `test`（`ubuntu@ylxh.de:22`）。
- 只读命令可直接执行。
- 改动命令必须先确认并加 `--write`。
- 护栏会把部分 `php artisan` 只读命令误判为 mutating；若语义只读，可在确认后用 `--write` 绕过。
- 另有生产服 `prod`（`ubuntu@129.226.211.33:22`），改动门槛更高，能在 `test` 先验证的先验证。

---

## 6. 部署 / 更新建议

### 6.1 安装 / 切换到指定版本（tag）

在 SeAT 根目录：

```bash
cd /var/www/seat
sudo composer require akinams053/seat-pap:2.0.0 --no-cache
sudo -u www-data php artisan vendor:publish --force --provider="Seat\Kassie\Calendar\CalendarServiceProvider"
sudo -u www-data php artisan migrate --force
sudo -u www-data php artisan view:clear
sudo -u www-data php artisan route:clear
sudo -u www-data php artisan cache:clear
```

### 6.2 升级到新版本（换 tag）

```bash
cd /var/www/seat
sudo composer require akinams053/seat-pap:<新版本，如 2.1.0> --no-cache
sudo -u www-data php artisan migrate --force
sudo -u www-data php artisan vendor:publish --force --provider="Seat\Kassie\Calendar\CalendarServiceProvider"
sudo -u www-data php artisan view:clear
sudo -u www-data php artisan route:clear
sudo -u www-data php artisan cache:clear
```

---

## 7. 接手人建议动作

1. 若继续联调外部抽奖服务，先读 [`06-API使用说明.md`](06-API使用说明.md)。
2. 上线后按 [`03-近期计划.md`](03-近期计划.md) 补做普通 operation / PAP 发放 / 行动审查 UI 目视回归。
3. 决定是否清理测试服 operation `181`。
4. 后续新功能从 `main` 新建分支，继续按 tag / Packagist 发布。
5. 若未来恢复商店，重新评估 `debit/refund` 与抽奖赊账之间的双花边界。

---

## 8. 生产部署记录（2026-06-06）

### 8.1 部署结论

生产服 `prod`（`ubuntu@129.226.211.33:22`）已完成从旧基线升级到 `2.0.0`：

```text
before: akinams053/seat-pap dev-dev-shop @ 98eab22c66c71363f716aca43948ecf963b0e9cf
after : akinams053/seat-pap 2.0.0      @ e771de7852bb5b261e31ac30a6d2ab2b54648893
```

部署按 [`07-生产部署计划.md`](07-生产部署计划.md) 执行：先进入维护模式、暂停 worker、做整库与整站快照，再安装 tag、迁移、发布资源、清缓存、验证后恢复服务。

### 8.2 备份与回退凭据

本次备份时间戳：

```text
TS=20260606_123158
```

生产备份文件：

```text
/var/backups/seat/seat_db_20260606_123158.sql
/var/backups/seat/seat_code_20260606_123158.tar.gz
/var/backups/seat/latest_seat_pap_deploy_ts.txt
/var/backups/seat/migrate_pretend_20260606_123158.log
/var/backups/seat/migrate_force_20260606_123158.log
```

已确认：

- DB dump 非空，约 `2.4G`，尾部有 `Dump completed on 2026-06-06 12:34:52`。
- 整站 tar 非空，约 `490M`，`tar -tzf` 完整性检查通过。
- 备份后根分区仍约 `11G` 可用。

如需回退，仍按 `07` 的物理快照方案执行：同一 `TS` 的代码 tar 与整库 dump 必须一起还原，不使用 `migrate:rollback`。

### 8.3 执行过的生产动作

维护与 worker：

```text
php artisan down --render="errors::503"
supervisorctl stop seat-horizon:seat-horizon-8000
```

部署：

```text
composer require akinams053/seat-pap:2.0.0 --no-cache --no-interaction
php artisan migrate --pretend
php artisan migrate --force
php artisan vendor:publish --force --provider="Seat\Kassie\Calendar\CalendarServiceProvider"
php artisan route:clear
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

恢复：

```text
supervisorctl start seat-horizon:seat-horizon-8000
php artisan up
```

生产 worker program 已确认是：

```text
seat-horizon:seat-horizon-8000
```

后续维护时优先只操作该 program，不建议无脑 `supervisorctl stop all/start all`。

### 8.4 迁移结果

本次 `migrate --force` 新跑 5 个迁移，均成功，batch 为 `[19]`：

```text
2026_05_29_100000_widen_pap_value_precision
2026_05_29_100100_extend_calendar_tags_analytics_enum
2026_05_29_100200_create_lottery_tables
2026_05_30_000000_cleanup_deprecated_notification_settings
2026_06_01_000000_add_bank_ledger_columns
```

`create_pap_audit_tables` 部署前已存在，状态仍为 `[11] Ran`。

### 8.5 服务端验证结果

部署后验证通过：

- `composer show akinams053/seat-pap` 显示 `2.0.0` / `e771de7852bb5b261e31ac30a6d2ab2b54648893`。
- 新路由存在：
  - `POST api/calendar/paps/lottery/settle`
  - `POST api/calendar/paps/debit`
  - `POST api/calendar/paps/refund`
  - `GET calendar/audit`
  - `GET calendar/audit/operations`
- 旧 `calendar/audit/consumption` 路由不存在。
- Laravel bootstrap 只读检查通过：`APP_BOOT=OK`。
- 新表存在：`kassie_calendar_lotteries`、`kassie_calendar_lottery_prizes`、`kassie_calendar_lottery_nodes`。
- 新字段存在：`calendar_operations.is_consumption`、`calendar_operations.consumption_key`、`kassie_calendar_pap_adjustments.source/external_ref/ref_group`。
- `calendar_tags.analytics` enum 已包含 `lottery`。
- `kassie_calendar_paps.value` 与 `kassie_calendar_pap_adjustments.value` 均为 `decimal(8,2)`。
- 当前生产无旧内置抽奖历史可回填，`lotteries` / `lottery_tags` / `consumption_operations` / `lottery_adjustments` 计数均为 `0`，符合预期。

恢复服务后验证：

```text
MAINTENANCE_MODE=OFF
seat-horizon:seat-horizon-8000 RUNNING
```

使用正确 SeAT 域名 `yeluo-xinghai.icu` 做本机 HTTPS 探测：

```text
https://yeluo-xinghai.icu/                    -> 302 /auth/login
https://yeluo-xinghai.icu/calendar/operation -> 302 /auth/login
GET /api/calendar/paps/123                    -> 401
GET /api/calendar/paps/lottery/settle         -> 405
```

这些结果符合未登录 / 未授权访问预期。

### 8.6 部署中发现的注意事项

- 不能用 `http://127.0.0.1/` 判断 SeAT 是否正常：该请求会命中另一个 nginx vhost `yeluo-xinghai.online`，反代到 `116.205.186.5:9999`，可能返回 `504`，与 SeAT 无关。生产本机探测 SeAT 时应使用正确 Host/SNI，例如：

  ```bash
  curl -k --resolve yeluo-xinghai.icu:443:127.0.0.1 https://yeluo-xinghai.icu/
  ```

- 本次操作中曾用进程列表检查备份进程，导致 DB 密码出现在会话工具输出中。没有写入仓库文档中的明文密码，但建议将本次会话 transcript 视为敏感材料，必要时后续安排轮换生产 DB 密码。
- `composer require` 后提示宿主依赖存在若干 security advisories；这不是本次 `seat-pap` 升级阻断项，未在维护窗口内展开处理，建议后续单独评估。

### 8.7 仍建议人工目视回归

服务端验证已通过，但以下需要登录态 / 浏览器 / 外部服务配合，建议上线后人工补扫：

- 登录后打开 `/calendar/operation`，确认 operation 列表正常加载。
- 打开 `/calendar/audit`，确认行动审查列表与弹窗正常。
- 抽查角色 PAP 页、军团 PAP 页。
- 设置页生成 PAP 商店 / 超网抽奖跳转，确认 JWT `exp - iat == 120`。
- 外部抽奖服务后续按 [`08-抽奖服务器对接指南.md`](08-抽奖服务器对接指南.md) 做真实 `settle` 联调。
